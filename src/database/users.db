import sqlite3 from 'sqlite3';
import { open } from 'sqlite';

(async () => {
  const db = await open({
    filename: './users.db',
    driver: sqlite3.Database,
  });

  await db.run(`CREATE TABLE IF NOT EXISTS users (username TEXT PRIMARY KEY, customRewardId TEXT)`);
})();
